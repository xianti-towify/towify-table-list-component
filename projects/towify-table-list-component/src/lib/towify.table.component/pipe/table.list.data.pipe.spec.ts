import { Table.List.DataPipe } from './table.list.data.pipe';

describe('Table.List.DataPipe', () => {
  it('create an instance', () => {
    const pipe = new Table.List.DataPipe();
    expect(pipe).toBeTruthy();
  });
});
